@model PilotSmithApp.UserInterface.Models.EmployeeViewModel
@if (@ViewBag.IsRequired == "required")
{
    <div class="input-group">
        @Html.DropDownList("EmployeeID", Model.EmployeeSelectList, "--Select employee--", new { @class = "form-control newinput", @required = "", @title = "The Employee is missing", @onchange = "$('#hdnEmployeeID').val(this.value)" })
        <span class="input-group-addon" style="cursor:pointer;color:#4bd24b;box-shadow:0px 1px 3px 0px #4bd24b" onclick="AddStateMaster('OTR')"><i class="fa fa-plus-square"></i></span>
    </div>
}
else
{
    <div class="input-group">
        @Html.DropDownList("EmployeeID", Model.EmployeeSelectList, "--Select employee--", new { @class = "form-control newinput", @onchange = "$('#hdnEmployeeID').val(this.value)" })
        <span class="input-group-addon" style="cursor:pointer;color:#4bd24b;box-shadow:0px 1px 3px 0px #4bd24b" onclick="AddStateMaster('OTR')"><i class="fa fa-plus-square"></i></span>
    </div>
}

@Html.ValidationMessage("EmployeeID", "", new { @class = "text-danger" })

<script>
    $(document).ready(function () {
        $('#EmployeeID').val($('#hdnEmployeeID').val());
    });
</script>


